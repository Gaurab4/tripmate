# Used only when running `npm run dev` (Vite mode: development).
# Leave unset so `src/api.js` uses relative /api URLs and vite.config.js proxies to 127.0.0.1:8000.
# Uncomment only if you need to call the backend without the proxy:
# VITE_API_URL=http://127.0.0.1:8000